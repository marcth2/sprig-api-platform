; Dev-only PHP-FPM pool override — loaded last (zzz- prefix) to override www.conf.
; Sets worker user/group to match the host user created via Dockerfile ARG UID/GID.
; This file is NOT copied in the ci or release stages.
[www]
user = app
group = app
