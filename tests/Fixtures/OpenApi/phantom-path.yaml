openapi: "3.0.0"
info:
  title: Test API
  version: "1.0.0"
servers:
  - url: "http://localhost:8000"
paths:
  /api/health:
    get:
      operationId: getHealthAggregate
      summary: All services health check
      security:
        - sanctum: []
      tags:
        - HealthCheck
      responses:
        "200":
          description: OK
        "401":
          description: Unauthenticated
        "503":
          description: Degraded
  /api/health/{service}:
    get:
      operationId: getServiceHealth
      summary: Single service health check
      security:
        - sanctum: []
      tags:
        - HealthCheck
      parameters:
        - name: service
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
        "401":
          description: Unauthenticated
        "404":
          description: Not found
        "503":
          description: Degraded
  /api/nonexistent:
    parameters: []
    get:
      operationId: getNonexistent
      summary: This route does not exist in the router
      responses:
        "200":
          description: OK
components:
  securitySchemes:
    sanctum:
      type: http
      scheme: bearer
